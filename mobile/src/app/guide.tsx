import { StubScreen } from "@/components/ui/stub-screen";

export default function GuideRoute() {
  return <StubScreen title="How to Use" eyebrow="Getting started" step="The in-app guide is ported near the end of the build." />;
}

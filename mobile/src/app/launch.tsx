import { StubScreen } from "@/components/ui/stub-screen";

export default function LaunchRoute() {
  return <StubScreen title="Launch Monitor" eyebrow="Garmin R10" step="Session import and shot metrics arrive in step 7." />;
}
